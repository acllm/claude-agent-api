apiVersion: v1
kind: Service
metadata:
  name: {{ include "claude-agent-api.fullname" . }}
  labels:
    {{- include "claude-agent-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "claude-agent-api.selectorLabels" . | nindent 4 }}
